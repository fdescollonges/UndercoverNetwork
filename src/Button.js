import React, { Component } from 'react';

class Button extends Component {
  render() {
    return <button type="button">Generate One-Time password</button>;
  }
}

export default Button; // Don’t forget to use export default!